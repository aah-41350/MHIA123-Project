{
  "name": "Codespace + Tailscale + PostgreSQL Proxy",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "vscode",
  "postCreateCommand": "sudo tailscaled --tun=userspace-networking --socks5-server=localhost:1055 --state=/var/lib/tailscale/tailscaled.state & sleep 3 && sudo tailscale up --authkey=${TS_AUTHKEY} --hostname=codespace-dev --netfilter-mode=off --accept-routes",
  "containerEnv": {
    "TS_AUTHKEY": "${localEnv:TS_AUTHKEY}"
  },
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {},
    "ghcr.io/devcontainers/features/git:1": {}
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker",
        "mtxr.sqltools",
        "matthewpi.sqltools-driver-pg"
      ]
    }
  }
}
